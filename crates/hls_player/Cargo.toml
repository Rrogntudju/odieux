[package]
name = "hls_player"
version = "0.1.0"
authors = ["Rrogntudju"]
edition = "2024"

[dependencies]
rodio = "0.22"
anyhow = "1"
hls_handler = {path = "../hls_handler"}

[features]
throttling = []

