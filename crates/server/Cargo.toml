[package]
name = "server"
version = "0.3.0"
authors = ["Rrogntudju"]
edition = "2021"

[dependencies]
axum = "0.6"
tower-http = {version = "0.3", features = ["fs", "timeout"]}
bytes = "1"
tokio = {version = "1", features = ["macros"]}
anyhow = "1"
serde = { version ="1", features = ["derive"] }
serde_json = "1"
reqwest = "0.11"
hls_player = {path = "../hls_player"}
media = {path = "../media"}
rand = "0.8"
once_cell = "1"